# Content Pipeline — Backlog

**Single source of truth for known issues and deferred work.**

---

## Known Issues

| ID | Issue | Status | Target |
|----|-------|--------|--------|
| K001 | ContentRenderer hardcoded status values (`duplicate`, `excluded`, `dead_link`, `DROP`) for flagging/pre-deselection. Should be schema-driven. | ✅ Fixed (Phase 10) — `flagged_when` wired through manifest → output_render_schema → ContentRenderer | Phase 10 |
| K002 | rss-feeds has no execute.js | Placeholder only | Phase 9+ |
| K003 | No transaction/locking on step approval route (runs.js:146-185). 3 sequential DB writes without atomicity. Extends to concurrent case. | ✅ Fixed (Phase 8b) | Phase 8b |
| K004 | No pagination for large result sets | ✅ Fixed (Phase 10) — ContentRenderer paginates at 50 rows, Prev/Next controls | Phase 10 |

## Code Review Findings (Gemini, Feb 15 2026)

### CRITICAL — Fix in Phase 8b (pre-Phase 9 gate)

| ID | Issue | File | Status |
|----|-------|------|--------|
| R001 | `handleNext` race: save fires, run starts without awaiting. Worker may read stale options. | SubmodulePanel.tsx:400-405 | ✅ Fixed (Phase 8b) |
| R002 | Orphaned pending row: if BullMQ enqueue fails, `submodule_runs` row stays `pending` forever, blocking future runs. | submoduleRuns.js:193-209 | ✅ Fixed (Phase 8b) |

### MEDIUM — Fix in Phase 8b or early Phase 10

| ID | Issue | File | Status |
|----|-------|------|--------|
| R003 | No global ErrorBoundary. Component render error = white screen crash. | main.tsx | ✅ Fixed (Phase 8b) |
| R004 | Imprecise query invalidation: `['latestSubmoduleRuns']` invalidated broadly instead of scoped to `[runId, stepIndex]`. | useSubmoduleRuns.ts:64 | ✅ Fixed (Phase 8b) |
| R005 | Previous Run Summary bar missing. Spec requires it (SKELETON_SPEC line 280), data fetched but never rendered. | SubmodulePanel.tsx | ✅ Fixed (Phase 10) |

### LOW — Phase 10 cleanup

| ID | Issue | File | Status |
|----|-------|------|--------|
| R006 | 17 console.log debug statements in production code. | submoduleRuns.js | ✅ Fixed (Phase 10) — all removed, console.error/warn kept |
| R007 | panelStore not reset on navigation. Stale panel state when switching runs. | panelStore.ts | ✅ Fixed (Phase 10) — resetPanel action + RunView useEffect |
| R008 | Synchronous CSV parsing (`csv-parse/sync`) blocks event loop on large files. | stepContext.js:14 | ✅ Fixed (Phase 8b) |
| R009 | Shared import path `../../../shared/stepConfig.js` outside src/ — works but fragile. | stepConfig.ts:1 | ✅ Fixed (Phase 8b) |

## Backlog

| ID | Task | Added |
|----|------|-------|
| B001 | URL cleanup after scraping (purge old discovered_urls) | 2026-01-30 |
| B002 | Project-level filter customization (custom exclude/include patterns) | 2026-01-30 |
| B003 | Re-run cascade invalidate (supersedes/needs_review columns) | 2026-02-01 |
| B004 | Option presets / saved profiles — skeleton-wide feature. Save named combinations of submodule options (e.g., "company_profile" keep/drop criteria), load them from a dropdown in the pane. Needs: DB table for presets, UI for save/load/delete, changes to options system. Benefits every submodule, critical for LLM-based modules with complex option sets. | 2026-02-17 |
| B005 | Failure notification system — notify user when entities fail during pipeline execution (dead URLs, HTTP errors, etc.). Support channels: email, Telegram, Slack webhook. Skeleton-level feature, not per-submodule. | 2026-02-17 |
| B006 | Reference docs in pipeline templates — when B004 (saved pipelines) is built, include project_reference_docs as part of the template. Copying a template to a new project should copy the reference docs too. | 2026-02-19 |
| B007 | source_submodule field on pool items — ~~CONFIRMED MISSING.~~ **FIXED (Phase 9c).** Approve route now stamps `source_submodule: subRun.submodule_id` on approved items. content-writer uses this to distinguish analysis from SEO plan items in pool. | 2026-02-19 |
| B008 | Browser scraper submodule — Puppeteer/Playwright-based scraper for JS-rendered SPAs. Handles pages where HTTP fetch returns empty shell (`<div id="root">`). Detection: page-scraper flags items with `status: "needs_browser"` when Readability extracts < 50 words from a 200 OK response. Browser scraper processes only those flagged items. Also handles cookie consent dismissal for sites that server-block without cookies. Separate Step 3 submodule (`browser-scraper`), not a replacement for page-scraper. | 2026-02-21 |
| B009 | SEO Planner — real FAQ research. Replace keyword-summary.md FAQ selection with actual search demand data. Use Google "People Also Ask" / search volume API to find the most-asked questions for each company in their primary categories. Requires new tool capability (tools.search or similar web search integration). Current MVP uses curated FAQ suggestions from keyword-summary.md matched to the company's categories. | 2026-02-23 |
| B010 | Step 5 content-writer — second writing pass. Current single-pass output is good but reads slightly mechanical. Add a revision pass where the LLM re-reads its own draft and rewrites for flow, transitions, and natural tone. MVP output is acceptable; this is a post-MVP polish item. | 2026-02-25 |
| B011 | Multi-provider LLM support in tools.ai — **Phase A (quick fix): READY.** Standardize 4 inconsistent manifests: intent-tagger + hallucination-detector (string→select dropdowns, short model names), tone-seo-editor + url-relevance (add presets_enabled). Manifest-only, ~10min. **Phase B (centralized registry): DESIGNED.** Create `server/config/llmProviders.js` as single source of truth. Manifests use `type: "model_select"` — server enriches to `select` with values from config. Adding a provider = 1 config entry + 1 env var, zero manifest changes. OpenAI-compatible providers (Mercury, Gemini) reuse existing openai API branch via `apiFormat: 'openai'`. Full architecture at `.claude/plans/purrfect-swimming-lynx.md`. 7 LLM modules: url-relevance, intent-tagger, content-analyzer, seo-planner, content-writer, tone-seo-editor, hallucination-detector. | 2026-02-25 |
| B012 | Prompt archive / option presets — **Phase A: DONE.** `option_presets` table, API routes (`/api/presets`), PresetField.tsx UI component, usePresets.ts hooks all built and live. Works on any textarea/text/select option with `presets_enabled: true`. Supports global + project-scoped presets, default marking. **Phase A gaps:** 4 modules missing `presets_enabled` on textareas (intent-tagger intents, hallucination-detector prompt, tone-seo-editor prompt, url-relevance keep/drop criteria) — covered by B011 Phase A quick fix. **Phase B: BACKLOG.** Wire `template_id` to a `templates` table so a template = collection of presets across all submodules (e.g., "Company Profile" template pre-selects prompts for content-analyzer + seo-planner + content-writer). Supersedes B004. | 2026-03-11 |
| B013 | Human-touch rewriting step (Step 5) — AI humanizer runs after tone-seo-editor as the final generation pass. Targets 7 AI writing patterns (formulaic transitions, monotonous rhythm, parallel paragraphs, generic qualifiers, mechanical lists, predictable openers, hedging phrases). Preserves all headings, citations, section count, FAQ/Meta format, and SEO keywords. Post-LLM validation catches structural damage. Options: rewrite_intensity (light/moderate/aggressive), temperature (0.6), model (Sonnet or Gemini). Supersedes B010. Research brief: `specs/submodule-briefs/step5-human-rewriter.md`. | 2026-04-15 |
| B014 | Affiliate program/site detection in company profiles — AI miscategorizes affiliate programs (e.g., Affiliaterise) and affiliate sites (e.g., PuntersLounge) into generic categories. Need explicit detection for: (a) affiliate programs/networks (platforms connecting operators with affiliates), and (b) affiliate review sites (sites promoting operators for commission). Could be addressed in content-analyzer prompts, seo-planner categorization, or content-writer instructions. | 2026-04-15 |
| B015 | LinkedIn Company Scraper (Step 3) — Fetch structured company data from LinkedIn company pages via Bright Data's LinkedIn Company Information API ($1.50/1K records). Returns: about, specialties, company size, industry, founded year, HQ, funding, employee sample, similar companies, recent posts, followers, logo. Direct scraping fails (auth wall) — Bright Data handles authenticated sessions at scale. ~$1.80 for full iGaming database (~1,200 companies). Depends on linkedin-discovery (Step 1) for URLs. Research brief: `specs/submodule-briefs/step3-linkedin-company-scraper.md`. | 2026-04-15 |
| B016 | LinkedIn Posts Scraper — second-pass enrichment (Step 3) — Dedicated Bright Data LinkedIn Posts Scraper API ($1.50/1K posts) for extended post history. Triggered by `loop-router` (Step 7) when Step 6 QA detects thin content (< 500 words, few citations). Fetches up to 50 posts per entity beyond the ~10 from B015 company scraper. Runs alongside `google-pse-curated-search` as a paired enrichment strategy. Expected ~10-20% trigger rate (~$11 for full database). Research brief: `specs/submodule-briefs/step3-linkedin-posts-scraper.md`. | 2026-04-15 |
| B017 | LinkedIn Profile Scraper (Step 3) — Fetch full personal profiles (summary, experience with descriptions, education) via ScrapeLinkedIn API ($0.01/profile). **Tested 2026-04-15:** returns complete experience descriptions for all roles — Bright Data returns null for same data. Uses AI agent that navigates LinkedIn DOM like a human. **Two modes:** (1) `bio` mode — entity is a person, scrape their profile for biographical content generation (industry bios, "People to Know" features, leadership spotlights); (2) `company_people` mode — entity is a company, scrape key executive profiles from B015 employee links for "Key People" sections. Cost: $0.01/profile, ~$20 for 2,000 industry figures or ~$30-48 for company enrichment. Account: `danieloskarsson@hotmail.com`. Research brief: `specs/submodule-briefs/step3-linkedin-profile-scraper.md`. | 2026-04-15 |
| B018 | Prompt-driven Step 8 bundling — Current Step 8 modules (especially `schema-org-injector`, `html-output`, `meta-output`) are hardwired to the company profile funnel. They read specific fields like `overview`, `credentials`, `key_facts.founded` and have hard `depends_on` for `json-output` + `content-analyzer`. Running a different content type (news articles, game reviews, event pages) produces empty/broken output. **What's needed:** Make Step 8 modules prompt-driven like Step 5 — support custom prompts, reference documents (`doc_selector`), and `presets_enabled` textareas. Same pipeline infrastructure, different output formats by swapping prompts/configs. `markdown-output` and `json-output` are already somewhat generic; the others need rework. CTO direction: "Have it like Step 5. Upload prompts and supporting documents. Use analytics for different things, same with SEO, or skip them." | 2026-04-16 |
| B019 | ~~Versioned Submodule Presets + Routing Overrides~~ **SUPERSEDED by B043 (Multi-Card Pattern).** Original concept: named preset bundles per submodule for router selection. Replaced in V5 by the Multi-Card Pattern with Per-Entity Instructions — stackable submodule cards with Round 1-4 variants, configured per template (not shared across templates). See `specs/ITERATION_PLAN_V5.md` Versioning Architecture section. | 2026-04-23 |
| B020 | Declarative QA Routing Rules (Per-Submodule Ownership) — Each QA submodule (Step 6) should declare in its manifest: (1) what constitutes failure, (2) which target card + round variant to route to on failure. Example: hallucination-detector declares `on_fail: { target_step: 1, target_card: "google-pse-curated-search", round: "v2" }`. Loop-router reads these declarations and applies them — it becomes an executor of per-submodule routing rules, not the owner of all routing logic. The Step 7 UI should show a dropdown per submodule: "On failure → send to [card] with [round variant]". Priority: **Medium** (depends on B043 Multi-Card Pattern being built first). | 2026-04-23 |
| B021 | Template Validation — QA Toggle / Submodule Presence Gating — When creating or editing pipeline templates, validation should reject invalid combinations (e.g., enabling QA steps without required upstream submodules, or toggling QA checks that reference missing submodules). Currently templates accept any configuration without checking logical consistency. Needs: template validation layer that checks submodule dependencies and QA toggle coherence before saving. Priority: **Medium**. | 2026-04-23 |
| B022 | Step 10 Review UI — Display Failed Entity Markers — The Step 10 Review UI must visually distinguish failed/flagged entities from successful ones. Entities that were routed back by loop-router (Step 7) and still failed after rework, or entities flagged for manual review, should have clear visual markers (color coding, icons, failure reason badges) so reviewers can prioritize attention. Needs: UI markers in ReviewView, failure metadata propagation from Step 7 routing decisions. Priority: **HIGH**. | 2026-04-23 |
| B024 | ✅ **FIXED (Phase 1)** — `flagged_when` filter blocks QA failures from reaching loop-router. Fix: skip `flagged_when` filtering during auto-execute for Step 6+ QA submodules. `flagged_when` UI pre-deselection remains for manual approval. Commit: skeleton `4cd1f2c`. | 2026-04-23 |
| B025 | Within-Run Escalation (two gates) — **Gate 1 (after Step 2):** Check each entity's discovered URL count after url-filter. If below threshold → run escalation submodules (google-pse-curated-search with broader lists, ai-discovery-scout). If STILL below `fail_threshold` → mark entity as `terminal_state='failed', failure_reason='escalation_floor'` and drop it immediately. **Gate 2 (after Step 4):** Sum word counts of scraped content. If below quality threshold → try different source types or flag for manual review. Saves all AI tokens at Steps 3-6 for entities with insufficient source material. DB schema already supports it (`entity_run_meta` free-text columns). Needs: escalation check in `autoExecutor.js`, `escalation_rules` config key in `execution_plan`. Designed 2026-04-22, refined in V5. Priority: **HIGH**. | 2026-04-23 |
| B026 | ✅ **FIXED (Phase 2)** — "Load More" Pagination + Browser Status Fallback. browser-crawler gains `load_more_selector`, `max_load_more_clicks`, `max_load_more_seconds` options. browserPool.js gains `clickSelector`/`maxClicks`/`maxClickSeconds` with content-change guard and wall-time budget. url-filter gains tiered HEAD→browser fallback with CHALLENGE_MARKERS marker-dominant alive check. Cost → medium. Commits: skeleton `0450400`, modules `e536e2f`. | 2026-04-24 |
| B023 | Per-Submodule Concurrency & Timeout Configuration — Two problems: (1) `WORKER_CONCURRENCY` is a global hardcoded value (default 2) in the stage worker — cheap text modules and expensive browser modules share the same parallelism. (2) Step-level timeouts (`ENTITY_TIMEOUT_FACTOR`, `DEFAULT_STEP_TIMEOUTS`) are hardcoded in `timeouts.js` — cannot be tuned without code changes. **Solution:** Make both concurrency and timeouts configurable per-submodule via manifest settings. Some submodules already have timeout-like settings in their options — this should be standardized across all modules. Manifest fields: `concurrency` (max parallel entities for this submodule), `entity_timeout` (seconds per entity for step timeout calculation). Suggested concurrency defaults by cost tier: cheap=10, medium=5, expensive=2. These override the global `WORKER_CONCURRENCY` on a per-queue basis. Step timeout should be `max(entities × max_submodule_entity_timeout, base)` where the factor comes from the slowest submodule in that step. Touches: BullMQ queue system (per-submodule queues or priority-based routing), stageWorker.js, manifest schema, COST_CONFIG in timeouts.js, autoExecutor.js timeout computation. Depends on: None. Priority: **High** (blocks production-scale runs with 100+ entities). | 2026-04-23 |
| B027 | ✅ **FIXED (Phase 1)** — page-links/deep-links Query String Bug. `resolveUrl()` now uses `new URL()` API, strips only tracking params (utm_*, fbclid, gclid, etc.) while preserving pagination params. Commit: modules `7c136e0`. | 2026-04-24 |
| B028 | New Discovery Submodules — ~~(a) google-pse-news, (b) google-pse-directories, (c) linkedin-discovery~~ **CUT in V5** — replaced by `google-pse-curated-search` (one configurable submodule with curated site lists). LinkedIn discovery deferred to B044. **(d)** `ai-discovery-scout` — LLM generates search strategies, classifies results by type (~$0.05/entity). Brief at `specs/submodule-briefs/step1-ai-discovery-scout.md`. Priority: **Medium** (remaining item is ai-discovery-scout only). | 2026-04-24 |
| B029 | Step 5 Generation Quality — Six improvements across all 4 iGaming Step 5 modules: **(1)** Add `temperature` option to content-analyzer (0.2), seo-planner (0.3), content-writer (0.4) manifests — currently none pass temperature. **(2)** Add `max_tokens` option to content-writer manifest (default 16384) — without this, long articles silently truncate. **(3)** Compact master_categories.md (remove redundant paragraph descriptions, keep slug + one-line), master_tags.md (remove descriptions that just lowercase the slug), keyword-summary.md (inject only matching categories, not all 50). **(4)** Smart keyword filtering — content-analyzer output identifies 2-3 primary categories; seo-planner/content-writer only inject keyword packs for THOSE categories instead of all 50. **(5)** Content-writer prompt improvements: explicit word count targets per section, citation density requirements, transition quality instructions. **(6)** tone-seo-editor: add `max_tokens` option. Estimated token savings from (3)+(4): ~60% reduction in reference doc context per entity. Priority: **High** (directly improves output quality and reduces LLM costs). | 2026-04-24 |
| B030 | Reference Doc Selector — Selectable uploads per run. Currently, all uploaded reference documents in a submodule are automatically included in every run. Users should be able to upload multiple documents and then select which ones to use for each run via checkboxes. Affects all submodules with `doc_selector` option type (content-analyzer, seo-planner, content-writer, tone-seo-editor). Skeleton-level UI change in the options panel. | 2026-04-26 |
| B031 | Hero Section Content Extraction — Scrapers drop hero/banner intro copy on Next.js and SPA sites. Readability classifies hero sections as non-content (splash/banner), losing descriptive paragraphs that explain what a company does. Confirmed on pronetgaming.com/en/retail: headline "Transform the Retail Experience" was kept, but the 3 paragraphs describing the platform were dropped. Critical for companies with thin sites where hero copy may be the primary content. Affects page-scraper (Readability) and potentially browser-scraper (content extraction step). Priority: **High** (content-thin companies lose key information). | 2026-04-26 |
| B032 | ✅ **FIXED (Phase 2)** — url-canonicalizer Transform Data Operation Bug. Transform now builds removalSet from both output URLs AND original_url. Canonicalizer deduplicates output by final canonical URL. Commits: skeleton `0450400`, modules `e536e2f`. | 2026-04-26 |
| B033 | Code Cleanup Batch — Collection of low-priority code-quality issues. Items: (1) url-filter execute.js: dead `browser undefined` fallback path (browser is always available via buildTools). Batch when list reaches 5-10 items. Priority: **Very Low**. | 2026-04-27 |
| B034 | Audit Codebase for Hardcoded Behavioral Constants — Grep skeleton + modules for numeric constants in control flow (timeouts, batch sizes, retry counts, thresholds). For each: decide if it should be a tunable manifest option with default. Examples: browserPool networkidle timeout (8s), url-filter HEAD/browser batch sizes (20/2), browser timeout (15s), LLM retry counts. Aligns with data-driven configuration principle. Priority: **Medium**. | 2026-04-27 |
| B035 | Anthropic Prompt Caching for Static Per-Run Content — Mark content identical across all entities in a run as cacheable (reference docs, format specs, tone guides, system prompts). Cache reads ~10% of normal cost, writes ~25% more, break-even after 2 uses. Pre-work: (1) audit where SDK calls happen (centralized helper vs per-submodule), (2) add per-LLM-call token logging, (3) baseline one full pipeline run. Affects Step 4/5/6 modules. Priority: **Medium-High**. | 2026-04-27 |
| B036 | URL-Based Bot Protection Detection — Current Cloudflare detection uses body content heuristics (CHALLENGE_MARKERS). Fragile for non-Cloudflare systems (DataDome, Imperva, Akamai, PerimeterX). Better: capture `page.url()` after navigation — challenge pages redirect to known endpoints (e.g., `cdn-cgi/challenge-platform/*`). Combine URL detection with current body heuristic for redundancy. Pre-work: survey 20-30 protected iGaming sites, log final URLs, build endpoint pattern list from observed data. Priority: **Medium**. | 2026-04-27 |
| B037 | DB-Backed Curated Lists with UI — Phase 3 ships curated lists as JSON files. Future: Supabase table with `name`, `description`, `items` (JSONB), UI for CRUD, AI-assisted list suggestions. Migration: JSON files become seed data, submodule switches from file read to table query. Open questions: global vs project-scoped, list versioning, cross-project sharing. Priority: **Medium-High**. | 2026-04-27 |
| B038 | Project-Level Tool API Key Configuration — Currently all API keys are global env vars. Future: per-project keys configured at Step 0 UI. Encrypted DB storage, env var fallback when project config absent. Enables multiple projects with different customer-owned keys, different LLM providers per project, key rotation without redeploy. Scope excludes infrastructure keys (Supabase, hosting). Priority: **Medium-High**. | 2026-04-27 |
| B039 | Token Cost Measurement Infrastructure — Per-LLM-call logging: input tokens, output tokens, cache_creation, cache_read, model, cost. Run representative pipeline, identify top 3 cost contributors. Required pre-work before any token optimization claims. The "60% savings" claim from D2 was unverified — this prevents future planning errors from unmeasured assumptions. Priority: **Low-Medium**. | 2026-04-27 |
| B040 | ✅ **FIXED** — Sitemap-Parser Browser Fallback for 403. Tiered HTTP→browser fallback for 403/429/503 on sitemap.xml. Fetches XML through headless browser, then parses normally. Cost → medium. Commit: modules `258710b`. | 2026-04-27 |
| B041 | Page-Links Browser Fallback for 403 — Same pattern as B040/B2. page-links gets 403 on Cloudflare sites (confirmed: askgamblers). When HTTP fetch fails with 403, retry homepage fetch via `browser.fetch()` and extract links from browser-rendered HTML. Affects: `page-links/execute.js`, manifest (cost → medium). Priority: **Medium** (browser-crawler covers this partially, but page-links is cheaper and runs first). |
| B042 | Bright Data KYC Verification for Full Residential Proxy Access — Zone `pipeline_1test` has **limited access** to the Residential Network. Bright Data restricts gambling/betting domains (askgamblers, bet365, pronetgaming, etc.) until KYC is completed. Current state: proxy works for non-restricted sites (lumtest, google, news), fails with `ERR_TUNNEL_CONNECTION_FAILED` for gambling domains. The direct-connection fallback handles this transparently, but residential proxy bypass would improve success rates on Cloudflare-heavy iGaming sites. **Action:** Complete "Verify for full access" KYC in Bright Data dashboard (24-48h approval). SSL certificate: select "Continue without SSL certificate" (browserPool.js already uses `ignoreHTTPSErrors: true`). After KYC approval, re-test gambling domains through proxy. Account: oskarssongreen@gmail.com, zone: pipeline_1test, $8/GB pay-as-you-go, balance: $19.99. Priority: **Medium** (blocking full proxy utility but pipeline works via direct fallback). | 2026-04-27 |
| B043 | Multi-Card Submodule Pattern (Architectural) — Pipeline-wide stackable submodule instances with round assignments. Per-entity instruction-based routing. Each card has Round 1 (default) + optional Round 2-4 (retry variants) with complete configuration (options, prompts, reference docs). Config lives in template's `execution_plan`, no cross-template sharing. Replaces V4's E3 (preset bundles). See Versioning Architecture in `specs/ITERATION_PLAN_V5.md` for full design. Substantial work, dedicated planning round needed. Priority: **High** (enables Step 7 routing to be genuinely useful). | 2026-04-26 |
| B044 | Dedicated LinkedIn Discovery Submodule — Deferred from V4 Track A (was A4/B028c). PSE-only LinkedIn discovery (via google-pse-curated-search with LinkedIn site list) confirmed sufficient for name-based use case. Dedicated submodule deferred until: criteria-based discovery without name starting point, recent activity discovery (Google indexes LinkedIn slowly), coverage gaps (PSE recall <80% on known names), profile-restricted users not findable via Google. Architecture options: Voyager API scraper, commercial LinkedIn data API (Proxycurl, Bright Data Datasets), browser scraping with Web Unlocker. Brief at `specs/submodule-briefs/step1-linkedin-discovery.md`. Priority: **Low-Medium** (currently speculative). | 2026-04-26 |
| B045 | V5 Plan Reconciliation Tracking — Track V4→V5 divergences: what shipped vs planned, what cut and why, what unplanned shipped and why. Serves as audit trail for future planning accuracy. One-time documentation task, not code. Priority: **Low**. | 2026-04-26 |
| B047 | Future Platform Capabilities (configurable pipeline composition, per-instance human gates, scheduled runs, notification channels, drag-and-drop UI) — Captured during template planning that exercised platform limits. Not active work — build only when a concrete template requires it. Full design at `content-pipeline/PLATFORM_DIRECTION.md`. Priority: **Future** (triggered by multi-pass templates: opinion pieces, news monitoring, market research). | 2026-05-04 |
| B048 | Server Upgrade CX22→CX32 for Pipeline Throughput — Current CX22 (4GB RAM) limits `WORKER_CONCURRENCY` to 2 (each Playwright instance uses 200-400MB). Upgrading to CX32 (8GB RAM, ~€4/mo extra) enables `WORKER_CONCURRENCY=4`, halving browser-scraper time (~60 min saved on 50-entity runs). Only helps RAM-bound steps (Step 3 scraping). LLM-bound steps (5, 6) unaffected — address separately via LLM parallelism tuning. Priority: **Medium** (cheap win, but only ~20% of total run time). | 2026-05-21 |
| B049 | LLM Step Parallelism Tuning — Steps 5 (Generation) and 6 (QA) are API-bound, not server-bound. Investigate increasing concurrent LLM calls per step (currently limited by worker concurrency). Options: batch API calls within a single worker job, increase `WORKER_CONCURRENCY` after B048, or pipeline LLM requests with request queuing. Bigger time savings than B048 (~30-45 min on 50-entity runs). Priority: **Medium**. | 2026-05-21 |
| B050 | Smart browser-scraper Skip — Skip browser-scraper for entities where page-scraper already gathered sufficient content (e.g., word count above threshold). Avoids unnecessary Playwright launches. Could save 30-50% of Step 3 time. Requires content sufficiency heuristic (word count, section count, or similar). Priority: **Low-Medium**. | 2026-05-21 |
| B051 | **Prompt Caching + In-Memory Blob Cache (Pipeline Cost + Time Optimization)** — **Status: Deferred until after Phase 3 (routing + multi-card pattern) ships and validates. Plan complete, implementation ready.** **Supersedes B035 and B039.** Reasoning for deferral: Phase 3 is the bigger functional win and deserves focused implementation; measurement is cleaner when changes ship sequentially so cost/time improvements can be attributed to specific changes; Phase 3 introduces loop passes which may change caching patterns — designing caching before observing routing behavior is premature. **Scope (ready for implementation when picked up): (1) Cost warning + visibility system** (NOT model lock — multi-provider exploration is a goal): `recommended_model` field in manifests (advisory, not enforced); warning logged when expensive model used contrary to recommendation; per-LLM-call logging of model, input tokens, output tokens, estimated cost; surfaces both cost spikes and latency per call. **(2) In-memory blob cache:** Map keyed by run_id in poolBlobs.js; hydrateItems() checks cache first, falls back to Supabase; cleanup on run completion; reduces Supabase IO, modest time savings. **(3) Phase 1 caching — high-volume single-module:** `anthropic-beta: prompt-caching-2024-07-31` header in ai.complete(); system param with cache_control for stable prefixes; apply to intent-tagger (300+ calls/run) and url-relevance (170 calls/run); time savings from cached prefix processing, modest cost savings. **(4) Phase 2 caching — Step 5 cross-submodule:** refactor content-analyzer, content-writer, hallucination-detector to place text_content in system message position consistently; cache_control on scraped content; within-entity cache hits across Step 5 modules; biggest Sonnet cost lever (content-writer stays on Sonnet, quality non-negotiable). **Constraints (locked in):** DO NOT add max_model clamp — would prevent multi-provider testing (DeepSeek, alternate OpenAI models planned); DO NOT test Haiku for content-writer — core site content quality is non-negotiable; content-writer stays on Sonnet/Opus. **Pre-work needed before implementation:** measure baseline costs and latency per call after Phase 3 ships; identify actual bottlenecks (API latency vs Supabase IO vs sequential processing). Priority: **Medium-High** (real cost and time impact, Phase 3 first). | 2026-05-21 |
| B046 | Progressive Save Destroys User-Defined Submodule Order — `sortSubmoduleIds()` (introduced in `c2be6c5`, 2026-04-19) re-sorts the `execution_plan.submodules_per_step` array on every progressive save and from-run template creation, overriding whatever order the user configured in the UI. Originally added to fix "random DB ordering" but went too far — should only sort when initially populating, not on every save. **Impact:** Step 2 submodules run in wrong order (url-dedup runs last instead of first, causing 4,600+ duplicates to flow through expensive HEAD checks and LLM classification). Affects all steps where execution order matters. **Fix:** `sortSubmoduleIds()` should be the default for NEW submodules only. Once `execution_plan` is stored, the array order is the user's authority — progressive save should preserve existing order, only sort newly appended submodules. Files: `server/services/moduleOrder.js`, `server/routes/runs.js:128`, `server/routes/templates.js:418`. Priority: **High** (causes timeout cascades in production runs). | 2026-05-04 |
| B062 | **Per-Provider Model Selector Pattern Needed When Additional Search Providers Ship** — `seo-planner` v2.1.0 added `perplexity_model` (sonar / sonar-pro / sonar-reasoning / sonar-reasoning-pro). The `search_provider` option already documents Gemini, Ahrefs, Semrush as future providers. When any of those ships, each will need its own model selector option (`gemini_model`, `ahrefs_model`, etc.) — different providers have non-overlapping model namespaces, and the manifest schema doesn't support conditional dropdowns. **Pattern to apply:** for each new `search_provider` value, add a sibling `<provider>_model` option with that provider's model list as `values`. The currently-active provider's model option drives the actual API call; other providers' model options are no-ops (UI-visible but ignored at runtime). **Why deferred:** no second provider exists yet; speculative until Gemini/Ahrefs/Semrush integration is committed. **Trigger to file as work:** when adding any non-Perplexity search provider to seo-planner (or any other submodule that calls `tools.ai.complete` with a search provider). Filed: 2026-06-07. Priority: **Low** (future-facing, no current consumer). | 2026-06-07 |
| B059 | **`keyword_research: false` Makes seo-planner's CRITICAL RULE Paradoxical** — `seo-planner` v2.1.0's main LLM prompt opens with: *"Every keyword you place into the plan MUST come from the keyword research block above. ... Trust the research, not your priors."* But the same submodule supports `keyword_research: false`, which disables Perplexity and leaves `{keyword_research}` as the fallback string `"No keyword research data available."` In that mode, the CRITICAL RULE tells the LLM to trust a non-existent source. The new fallback instruction in the INPUTS section (added 2026-06-07) tells the LLM what to do (emit a HIGH-PRIORITY warning, tag keywords as `analysis`, proceed using categories/tags), but the CRITICAL RULE itself still reads as if research must exist. **Suggested fix:** `buildPrompt()` in `execute.js` conditionally strips or rewrites the CRITICAL RULE section when `keywordResearchText` is empty. Could be a sed-style block-strip on a `<!-- only-when-research -->` marker pair in the prompt template. **Why deferred:** the v2.1.0 fallback instruction is sufficient for non-silent failure — the LLM correctly emits the warning even with the paradoxical rule above it. The buildPrompt conditional is polish, not correctness. **Why blocking V5 Phase 3:** Phase 3 introduces new generic prompt templates (qa-structural, humanizer, qa-ai-detector). The same CRITICAL-RULE-style integrity rules will appear in those prompts. The conditional-section pattern needs to ship before Phase 3 authors copy-paste prompts that have the same paradox. Filed: 2026-06-07. Priority: **Medium** (polish on seo-planner; pattern-establishing for Phase 3). | 2026-06-07 |
| B058 | **Step 5 Peer Modules Still Hardcode iGaming/B2B/OnlyiGaming Framing in Manifest Defaults** — modules-v2 CLAUDE.md (May 22 commitment): *"OnlyiGaming-specific prompts belong in presets/reference docs per template run, not in module defaults."* `seo-planner` v2.1.0 (commit `e7be37f`, 2026-06-07) and `tone-seo-editor` v1.2.0 (commit `a24464b`, parallel session, 2026-06-07) both hold this line — manifest defaults are pipeline-agnostic. **But the remaining two Step 5 modules still violate the commitment in their COMMITTED defaults:** (a) `content-analyzer/manifest.json` default prompt: *"You are an iGaming industry analyst. Your task is to extract structured facts from source content about a company."* — locks to iGaming-companies. (b) `content-writer/manifest.json` default prompt: *"You are a professional content writer for OnlyiGaming, a B2B directory for the iGaming industry. ... company profile..."* — locks to OnlyiGaming-directory-company-profiles. **Asymmetry:** seo-planner and tone-seo-editor authors now write template preset overrides for OnlyiGaming-specific framing; content-analyzer and content-writer authors don't (because the manifest defaults already have the framing). When a non-iGaming content type ships (News, Podcasts, Marketplace, Reviews), those manifest defaults will need to be overridden, and the pattern asymmetry will produce confusion. **Fix shape:** rewrite the two manifest defaults to be pipeline-agnostic following the seo-planner v2.1.0 / tone-seo-editor v1.2.0 pattern. Move OnlyiGaming-specific framing to the `30 april` template's preset_map overrides. **Why blocking V5 Phase 3:** Phase 3 introduces new content-type submodules and new templates. New manifest authors will copy the existing iGaming-locked content-analyzer/content-writer defaults as a pattern, perpetuating the architectural drift. Fix before Phase 3 starts. Filed: 2026-06-07. Priority: **High** (architectural commitment compliance + Phase 3 entry gate). | 2026-06-07 |
| B056 | **`flagged_when` Semantics Incompatible with `transform` Data Operations (url-canonicalizer Drops Its Own Transformations)** — Observed 2026-06-06 on prod run `7dcc4794-5189-4511-8128-ff76bb0c0ec5`, entity Pronet Gaming: url-canonicalizer received 170 URLs from url-dedup, processed them (152 distinct after its internal redirect-dedup), but only **141** items reached its `approved_items` array. The 11 missing items were the URLs canonicalizer actually transformed (status='redirected' — e.g., `/blog/foo/` → `/en/blog/foo`). url-filter then saw the un-canonicalized originals + duplicates instead of the canonical replacements. **Root cause:** url-canonicalizer's manifest declares `output_schema.flagged_when: { "status": ["redirected"] }`. The skeleton's `/approve` route at `server/routes/submoduleRuns.js:1175-1188` uses `flagged_when` to filter items OUT of `approvedKeySet` BEFORE building it — this works correctly for `remove` ops where "approved = kept" (e.g., url-dedup's `flagged_when: { "status": ["duplicate"] }` means "don't keep duplicates"). But for `transform` ops where "approved = modified to canonical", `flagged_when: ["redirected"]` semantically reads as "don't pin the redirect target to the pool" — which contradicts the entire purpose of canonicalization. Net effect: canonicalizer correctly DETECTS the 11 redirects but the approve route silently drops them; the 11 original (pre-redirect) URLs stay in the pool; the 11 canonical URLs never reach the pool. **Impact:** Step 3 scrapers fetch the original URLs; HTTP follows the redirect transparently and the content is the same, so quality is unaffected — but every canonicalized URL costs an extra HTTP round-trip at scrape time. Bounded waste. Bug predates B054 — has been silently wrong since canonicalizer manifest was first authored. **Fix shape (Option A, RECOMMENDED):** modules-v2 manifest change — `modules/step-2-validation/url-canonicalizer/manifest.json` — remove `flagged_when` entirely, OR change to `flagged_when: { "status": ["error"] }` (currently no items emit status='error', so equivalent to removing). 1-line change. **Alternative (Option B, rejected):** skeleton change — make `flagged_when` semantics conditional on `data_operation` (only filter for `remove`, pass through for `transform`). Architecturally cleaner BUT affects all current and future `transform` submodules; needs a callsite audit (Pattern I) before shipping. Option A is sufficient for the observed bug. **Why blocking V5 Phase 3:** Phase 3 introduces new submodules (qa-structural, humanizer, etc.) — manifest authors will copy-paste `flagged_when` semantics from existing modules. Until the `flagged_when` semantics are clarified for `transform` ops (either via Option B fix OR via explicit docstring rule), new `transform` submodules will inherit the same trap. Fix B056 with Option A; document the `flagged_when` rule in modules-v2 CLAUDE.md Rule 12 as part of B055's audit output. Filed: 2026-06-06. **STATUS: documented, not yet fixed.** Priority: **Medium** (bounded waste, not a correctness issue; blocking Phase 3 because manifest authors will repeat the trap). | 2026-06-06 |
| B055 | **Phase 3 Entry Sweep: Skeleton Stability Audit** — Three skeleton bugs (B052 onConflict stale, B053 UI display, B054 pool dedup) surfaced in 24h of careful testing on 2026-06-06. CTO assessment: this is the visible part of an iceberg — architectural changes (Section C Multi-Card Pattern 2026-06-03, V5 add composite-key 2026-05-24) shipped without full call-site audits. Before V5 Phase 3 (4-6 weeks of UI + QA submodule work) begins, run a focused skeleton stability audit: (1) onConflict audit — grep all `.upsert(..., { onConflict: '...' })` against current DB unique indexes, catch any other stale strings beyond B052; (2) data_operation consumer audit — for each `data_operation` value (add/transform/remove), verify the actual DB pool state matches the documented contract under multi-source / multi-loop / multi-card conditions, catch any other latent bugs beyond B054; (3) UI count audit — for each per-step results display, verify the displayed numbers match the actual `entity_stage_pool` pool size at the corresponding boundary, catch any other display drift beyond B053. Estimated: 1-2 days of focused audit. Output: zero-or-N additional backlog items + a checklist proving the audit is complete. Treat as Phase 3 entry gate, not part of Phase 3 scope. Priority: **High** (de-risks 4-6 weeks of Phase 3 work). Filed: 2026-06-06. | 2026-06-06 |
| B054 | **applyDataOperation `remove` + `transform` Failed to Collapse Multi-Source Duplicates (Pool Bloat)** — Observed 2026-06-06 on prod run `844d4c1a-5868-4bcf-b8cd-f671b26462a3`, entity "Pronet Gaming": Step 1 pool had 258 items / 182 unique URLs (76 multi-source raw-URL duplicates from browser-crawler + deep-links + page-links discovering same URLs); Step 2 pool had 205 items / 168 unique URLs (37 raw-URL duplicates STILL surviving every Step 2 filter). **Root cause:** Step 1 `data_operation: 'add'` writes one row per composite `(itemKey, source_submodule)` — multi-source provenance preserved at discovery time, intentional. Step 2+ `data_operation: 'remove'` filters by `approvedKeySet.has(item[itemKey])` alone — when N pool rows share the same itemKey, all N pass the filter. Same latent bug in `transform` branch, currently masked because url-dedup (sort_order=1) runs before url-canonicalizer (sort_order=2). **Impact:** Step 3 scrapers iterate per-pool-item, so 168 unique URLs × 1.2 duplicate factor = 205 HTTP requests + Playwright launches + bandwidth. Content from duplicates flows into Step 5 LLM generation = redundant tokens. Step 6 QA = redundant tokens. Bug has been in prod since commit `4407a77` (May 24, ~13 days). User reported "the numbers don't add up" — math constraint: if url-dedup says 89 of 258 are duplicates, MAX downstream output is 169 unique. 205 is impossible without pool-persistence bug. **Fix (2026-06-06, commit `d45c16c`, deployed):** add `seen` Set to both `remove` AND `transform` branches in `server/lib/applyDataOperation.js`. First-occurrence-wins semantic documented in docstring. 13 new test cases in `server/tests/data-operations.test.mjs` covering multi-source dedup in both branches + regression guards for single-source pools + redirect canonicalization. 36/36 tests pass. Pre-fix rollback tag `pre-applyDataOperation-fix-2026-06-06` on skeleton at `4cebc74`. CTO + code-reviewer both approved fix shape; CTO recommended Pattern I (post-merge callsite audit) as broader systemic discipline — filed separately as modules-v2 process discipline addition. Why blocking V5 Phase 3 start: Phase 3 introduces new submodules (qa-structural, humanizer, qa-ai-detector) that may use `add`/`transform`/`remove` — building on a recently-fixed contract is safer than building on a still-broken one. Filed: 2026-06-06. **STATUS: FIXED + DEPLOYED**. | 2026-06-06 |
| B053 | **Step 2 UI Display Doesn't Reflect Actual Pool State Between Submodules** — Observed 2026-06-06 on Pronet+Wazdan test run `844d4c1a-5868-4bcf-b8cd-f671b26462a3`. UI showed per-submodule counts for Pronet Gaming at Step 2 that do not chain serially: url-dedup `258 → 169 approved`, url-canonicalizer `151 received → 140 approved`, url-filter `206 received → 169 approved`, url-relevance `206 received → 168 approved`. The numbers 258→151→206→206 don't make sense as a filter pipeline (pool can't grow between filters). **What's actually happening (verified via DB):** `entity_stage_pool` for Pronet Gaming went from 258 (Step 1 output) → 206 (Step 2 final output). The actual pool was filtered correctly from 258 → 206 across all 4 Step 2 submodules. The next step receives 206 URLs. **What the UI displays:** Each submodule's `approved_items` array from `entity_submodule_runs` — which is a per-submodule self-report ("of what I saw, here's what I approved"), not the running pool state between submodules. The "X in working pool" badge appears to show one snapshot moment (final pool size) regardless of submodule, while per-submodule rows show that submodule's internal accounting. **Impact:** Operator confusion when debugging — the displayed numbers suggest the pipeline is broken (counts don't chain) when actually the pool is being filtered correctly. Worsens the "I can't trust what I see" feeling for users investigating run behavior. **Suspected files:** UI components that render the Results table on Step 2 submodules (likely `client/src/components/shared/SubmodulePanel.tsx` or the ContentRenderer for the validation step). Server-side: how `entity_submodule_runs.input_data` is populated for downstream submodules vs how the UI badge is calculated. **Fix shape (TBD):** Either (a) UI should show actual pool size at submodule's execution moment (read from `entity_stage_pool` history if preserved, or from previous submodule's output snapshot), OR (b) UI should show per-submodule self-report with a clear label distinguishing "what this submodule saw/approved" from "running pool size." Option (b) is cheaper; option (a) is more correct. **Why blocking V5 Phase 3:** Phase 3's Multi-Card Pattern adds more complexity to per-step results display (round indicators, card identity). Building on top of an already-confusing per-submodule display layer will compound. Fix or relabel the display BEFORE Phase 3 UI work increases display complexity. Priority: **Medium-High** (operator trust issue, not pipeline correctness). Filed: 2026-06-06. | 2026-06-06 |
| B052 | **Multi-Card Pattern Migration Left onConflict Strings Stale (UI Save Failures)** — Sub-plan 1's `migration_multi_card_pattern.sql` (commit `376022d`, applied to prod 2026-06-03) replaced three unique indexes to include `card_id`: (a) `run_submodule_config`: `(run_id, step_index, submodule_id)` → `(run_id, step_index, submodule_id, COALESCE(card_id, ''))`; (b) `submodule_runs`: `idx_one_active_run_per_submodule` → `idx_one_active_run_per_submodule_card` adding `COALESCE(card_id, '')` to the partial UNIQUE; (c) `entity_submodule_runs`: `idx_entity_submodule_runs_one_active` → `idx_entity_submodule_runs_one_active_card` adding `COALESCE(card_id, '')` and `loop_iteration`. **But application code at three call sites still uses the OLD column set:** `server/routes/submoduleConfig.js:58`, `server/routes/submoduleRuns.js:269`, `server/routes/templates.js:542` all call `onConflict: 'run_id,step_index,submodule_id'`. **Symptom (observed 2026-06-06 on prod, Pronet+Wazdan test run):** When the user saves submodule options in the UI during a run, a toast appears: `there is no unique or exclusion constraint matching the ON CONFLICT specification`. The run otherwise proceeds correctly using template defaults; only per-run option customizations fail to persist. Verified by querying `run_submodule_config` rows for the active run — count was 0 despite UI save attempts. **Scope:** Three onConflict strings to update. Also need to decide handling of NULL `card_id` for Round-1 (default) entries — current index uses `COALESCE(card_id::text, '')`, but Supabase JS `onConflict` likely compares with NULL semantics. Options: (i) recreate indexes with `NULLS NOT DISTINCT` (Postgres 15+; prod DB is 17.6 so available), (ii) use a sentinel UUID value for Round 1, (iii) emit explicit empty string from app code. Option (i) cleanest. **Impact:** Degraded UX — per-run option customizations silently fail. Runs themselves succeed on template defaults (because falling back to template defaults is the existing behavior when no run_submodule_config row exists). Not a pipeline-execution blocker, but a user-trust blocker (errors in UI without explanation). **Why not blocking sub-plan 1 conclusion:** All known testing happened with template defaults; per-run UI edits weren't exercised in sub-plan 1's mechanism tests. **Why blocking V5 Phase 3 start:** Phase 3 introduces card stacking UI (#16) and routing rules editor UI (#17). Both features WILL save per-run/per-template config via the same onConflict path. Shipping Phase 3 UI on top of a broken save path means every UI interaction errors. Fix before Phase 3 UI work begins. **Fix scope:** Three string changes + one DB index migration (use `NULLS NOT DISTINCT` or sentinel). ~30-60 min. Priority: **High** (blocks Phase 3). | 2026-06-06 |

## Phase 9 Test Findings

| ID | Issue | Severity | Status |
|----|-------|----------|--------|
| P9-001 | tools.http has no HEAD method | Medium | ✅ Fixed — `head()` method added to tools.http in stageWorker.js |
| P9-002 | rss-feeds full-parse vs cost:cheap tension | Low | Open — untested (rss-feeds has no execute.js yet) |
| P9-003 | Step 1→2 data handoff | Resolved | ✅ Fixed — data-operation-aware input resolution, entity re-grouping from flat pool |
| P9-004 | ＝/➖/➕ data operation semantics undefined | Resolved | ✅ Fixed — ＝=independent/accumulate, ➖=chain/filter, ➕=chain/enrich |
| P9-005 | Sibling submodule chaining (Step 2) | Resolved | ✅ Fixed — ➖ submodules receive working pool, not original step input |
| P9-006 | Flagged items not pre-deselected | Resolved | ✅ Fixed — duplicate/excluded/dead_link/DROP auto-deselected on completion |
| P9-007 | URLs not clickable in result lists | Resolved | ✅ Fixed — ContentRenderer renders url columns as `<a>` links |
| P9-008 | Textarea input not shared across siblings | Resolved | ✅ Fixed — textarea save also upserts to step_context |
| P9-009 | UI not updating after approval (pool count, sibling data) | Resolved | ✅ Fixed — run query invalidated after submodule approval |
| P9-010 | Cannot re-approve a completed step | Resolved | ✅ Fixed — Reopen Step feature added |
| P9-011 | tools.ai missing from worker | Resolved | ✅ Fixed — stageWorker.js exposes tools.ai.complete() for LLM submodules |

---

## Submodule Development Roadmap (added 2026-03-20)

**Decision:** Submodules can be specced and built in parallel with skeleton work. They are pure functions (`input.entity` in, `{ entity_name, items, meta }` out), live in the modules repo, and don't touch the skeleton.

**32 research briefs** ready at `specs/submodule-briefs/`. Each brief follows the research brief template and can be handed to any developer independently.

| Step | Count | Submodules |
|------|-------|------------|
| 1 — Discovery | 7 | ai-discovery-scout, google-pse-curated-search (replaces ~~google-pse-news~~, ~~google-pse-directories~~), seed-url-builder, ~~linkedin-discovery~~ (deferred to B044), youtube-podcast-discovery, social-media-discovery, curated-list-import, image-logo-search |
| 2 — Validation | 1 | learned-validator |
| 3 — Scraping | 5 | media-transcript-fetcher, api-data-fetcher, linkedin-company-scraper, linkedin-posts-scraper, linkedin-profile-scraper |
| 4 — Filtering | 2 | boilerplate-stripper, intent-tagger |
| 5 — Generation | 6 | seo-keyword-researcher, tone-seo-editor, human-rewriter, image-generator, video-generator, audio-tts-generator |
| 6 — QA | 4 | keyword-sufficiency-checker, meta-compliance-checker, citation-coverage-checker, hallucination-detector |
| 8 — Bundling | 1 | schema-org-injector |
| 9 — Distribution | 3 | strapi-publisher, google-docs-exporter, google-sheets-logger |
| 7 — Routing | 1 | loop-router |

**Key corrections from original plan:**
- PSE Directories = one configurable submodule (not one per directory)
- Curated List Import = separate from PSE (imports pre-built Google Sheets lists)
- AI Discovery Scout runs first (generates leads for downstream submodules)
- Image & Logo Search added to Step 1 (was missing)
- SEO Keyword Researcher uses real tools (Ahrefs/SERPApi/GSC), not LLM-guessed keywords
- Media Transcript Fetcher moved from Step 5 → Step 3 (scraping is where it belongs)
- Step 5 media enrichment split into 3: Image Generator, Video Generator, Audio/TTS Generator

---

*Updated: 2026-05-21 — B048-B051 added (server upgrade, LLM parallelism, smart scraper skip, prompt caching + blob cache). B051 supersedes B035 (Anthropic prompt caching) and B039 (token cost measurement). B043-B045 added (Multi-Card Pattern, dedicated LinkedIn discovery, V5 reconciliation tracking). B024 marked FIXED (Phase 1, skeleton 4cd1f2c). B040 marked FIXED (modules 258710b). B019 superseded by B043 (Multi-Card Pattern replaces preset bundles). B028 (a)/(b)/(c) CUT, replaced by google-pse-curated-search + B044. B025 updated to two-gate design (V5). B020 updated for Multi-Card terminology. B016 updated (google-pse-news → google-pse-curated-search). Superseded items: B004 (by B012), B010 (by B013), B019 (by B043), B035 (by B051), B039 (by B051). Fixed: B007, B024, B026, B027, B032, B040. Remaining open: P9-002, B001-B003, B005-B006, B008-B009, B011-B018, B020-B023, B025, B028(d only), B029-B031, B033-B034, B036-B038, B041-B051.*
